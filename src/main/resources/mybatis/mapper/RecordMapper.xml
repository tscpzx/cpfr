<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ts.cpfr.dao.RecordDao">
	<select id="selectRecordListWithBlob" parameterType="pd" resultType="pd">
		SELECT r.record_id,p.person_name,d.device_name,r.record_time,r.record_image base_image,r.recog_type FROM tbl_#{wid}_person p RIGHT JOIN tbl_#{wid}_record r ON p.person_id=r.person_id  LEFT JOIN tbl_#{wid}_device d ON r.device_id=d.device_id
	</select>
</mapper>